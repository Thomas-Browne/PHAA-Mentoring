# Install packages
# Press Ctrl + A, and Ctrl + Enter to install the packages required to run the scripts. This only needs to be done once per computer.

install.packages('tidyverse')
install.packages('officedown')
install.packages('pdftools')
install.packages('readxl')
install.packages('purrr')
